apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-copy-from-pvc
spec:
  storageClassName: csi-qsd
  dataSource:
    name: pvc
    kind: PersistentVolumeClaim
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
